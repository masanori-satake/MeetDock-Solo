# AGENTS.md

このファイルは、本リポジトリで作業を行うAIエージェント（Jules等）向けの開発・運用ガイドラインです。

## 📌 バージョン更新およびリリースルール（必須）

本プロジェクトでは、コードやドキュメント（VSIXに含まれる全ファイル）に修正・機能拡張・不具合修正を行う際、**必ず適切なバージョン昇格（インクリメント）を行うこと**が義務付けられています。
pre-commit フックおよび CI にてバージョン更新チェックが実行されます。

### 1. バージョン選択基準 (SemVer)
修正内容に応じて、以下の規則でバージョンを更新してください：

- **マイナーバージョン (`X.Y.Z` -> `X.(Y+1).0`)**:
  - 新機能の追加、既存機能の拡張、仕様変更などの場合
- **パッチバージョン (`X.Y.Z` -> `X.Y.(Z+1)`)**:
  - 不具合修正（バグフィックス）、軽微なUI調整、`README.md` などのパッケージ包含ドキュメントの更新の場合

※例外：`.vscode/`, `.vscode-test/`, `.github/`, `docs/`, `scripts/`, `.gitignore`, `tsconfig.json` 等のVSIXパッケージ外の開発用設定・ドキュメントのみの更新の場合はバージョン昇格不要です。

### 2. バージョン更新手順
コードまたはドキュメントを修正したら、提出前（コミット前）に必ず以下のコマンドを実行して全関連ファイルのバージョンを一括更新してください。

```bash
npm run update-version <new_version>
```
（例: `npm run update-version 1.5.2`）

このコマンドは `package.json`, `package-lock.json`, `README.md`, `CHANGELOG.md` のバージョン表記を連動して更新します。

### 3. CHANGELOG.md の記述
`npm run update-version` 実行後、`CHANGELOG.md` に自動生成された新しいリリース項目の下に、行った変更内容の概要を**日本語**でわかりやすく記述・更新してください。

### 4. バージョンチェックの検証
提出前に、以下のコマンドでバージョン不整合および更新忘れがないか確認してください。

```bash
npm run check-version
# または Git 差分に対するバージョン昇格チェック
npm run check-version-bump
```

---

## 🛠 開発・テストガイドライン

- テスト・ビルド実行:
  - `npm run compile`
  - `npm run check-types`
  - `npm run lint`
  - `xvfb-run npm test` (Linuxヘッドレス環境の場合)
- コミットメッセージ・PRタイトル・説明文はすべて日本語で記述すること。
